title: Learned & Adaptive Cloud Indexing
status: Open
area: Pure DB

## Motivation

Cloud databases face shifting workloads and data placement costs where static indexes are either wasteful or missing exactly when needed.

## Goal

Instance-optimality in the cloud via learned, adaptive indexing — build indexes incrementally and adaptively as queries arrive, approaching the performance of an ideal index chosen in hindsight.

## Timeline
